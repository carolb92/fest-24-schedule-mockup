// import schedule from "@/data/events.json";

export default function SearchContent() {
	// TODO: search/filter `schedule` by performer
	return <span className="text-white">search for a performer</span>;
}
